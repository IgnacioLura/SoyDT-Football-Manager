[workspace]

[package]
name = "engine_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
core = { path = "../../open-football/src/core" }
database = { path = "../../open-football/src/database" }
chrono = "0.4.45"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bincode = "1"
base64 = "0.22"
